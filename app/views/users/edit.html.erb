<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

<h2>User info</h2>
  <%= form_with model: @user, url: user_path(@user), method: :patch do |f| %>
  
  <% if @user.errors.any? %>
  <%= @user.errors.count %>error prohibited this obj form being saved:
  <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
  <% end %>
  </ul>
  <% end %>
  
    <div class="form-group"><h4>Name</h4>
      <%= f.text_field :name %>
    </div>
    <div class="form-group"><h4>Image</h4></h4>
      <%= f.file_field :profile_image, placeholder: "ファイルを選択", accept: "image/*" %>
    </div>
    <div class="form-group"><h4>Introduction</h4>
      <%= f.text_area :introduction %>
    </div>
    <div class="form-group">
      <%= f.submit 'Update User', class: 'btn btn-secondary' %>
    <% end %>
    </div>
